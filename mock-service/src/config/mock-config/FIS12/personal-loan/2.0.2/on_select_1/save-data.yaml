save-data:
  transaction_id: "$.context.transaction_id"
  message_id: "$.context.message_id"
  kyc_verification_status: "$..form.url"
  items: "$.message.order.providers[*].items[*]"
  provider_id: "$.message.order.providers[0].id"
  item: "$.message.order.providers[0].items[0]"
  consent_handler: "$.message.order.items[0].tags[0].list[0].value"
  # Loan data for dynamic SETTLEMENT_AMOUNT calculation
  quote_price: "$.message.order.quote.price.value"
  principal_amount: "$.message.order.quote.breakup[?(@.title=='PRINCIPAL')].price.value"
  loan_term: "$.message.order.items[0].tags[?(@.descriptor.code=='INFO')].list[?(@.descriptor.code=='TERM')].value"
